CREATE TABLE IF NOT EXISTS librarians (id INTEGER PRIMARY KEY AUTO_INCREMENT ,login TEXT,password TEXT,name TEXT NOT NULL,phone TEXT,address TEXT);
CREATE TABLE IF NOT EXISTS sessions (id VARCHAR(32) PRIMARY KEY, user_id INTEGER NOT NULL);